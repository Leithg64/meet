# "org" ensures this Service is used with the correct Serverless Framework Access Key.
org: leithg
# "app" enables Serverless Framework Dashboard features and sharing them with other Services.
app: meet
# "service" is the name of this project. This will also be added to your AWS resource names.
service: auth-server

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-central-1
  timeout: 30
  environment:
    CLIENT_ID: ${file(config.json):504557396693-n31ars4hr2hqrhq60iuasb0ali9tromo.apps.googleusercontent.com}
    CLIENT_SECRET: ${file(config.json):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}
    CALENDAR_ID: ${file(config.json):fullstackwebdev@careerfoundry.com}
functions:
  getAuthURL:
    handler: handler.getAuthURL
    events:
      - http:
          path: api/get-auth-url
          method: get
